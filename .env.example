############################
# Server
############################
# Host and port for Strapi
HOST=0.0.0.0
PORT=1337

# Public URL of your Strapi instance
# For local dev: http://localhost:1337
# For Render (after deploy): https://<your-service>.onrender.com
PUBLIC_URL=http://localhost:1337

############################
# Strapi Secrets
############################
# Generate each with: openssl rand -base64 32
APP_KEYS=toBeModified1,toBeModified2
API_TOKEN_SALT=toBeModified
ADMIN_JWT_SECRET=toBeModified
TRANSFER_TOKEN_SALT=toBeModified
JWT_SECRET=toBeModified
ENCRYPTION_KEY=toBeModified

############################
# Database
############################
# --- Local Development (SQLite) ---
DATABASE_CLIENT=sqlite
DATABASE_FILENAME=.tmp/data.db

# --- Production on Render + Supabase (Postgres) ---
# DATABASE_URL=postgres://user:password@host:5432/dbname
# DATABASE_SSL=true

############################
# Twilio (SMS Integration)
############################
# Get these from Twilio dashboard
TWILIO_ACCOUNT_SID=toBeModified
TWILIO_AUTH_TOKEN=toBeModified
TWILIO_FROM=+10000000000
SMS_DEFAULT_COUNTRY=61

############################
# Frontend URL
############################
# For local dev: http://localhost:3000
# For production (Vercel): https://<your-frontend>.vercel.app
FRONTEND_URL=http://localhost:3000

############################
# Upload Provider (optional for production)
############################
# If using S3 / Cloudflare R2 / Supabase Storage, configure these:

# --- S3 / Cloudflare R2 ---
# S3_ENDPOINT=https://<accountid>.r2.cloudflarestorage.com
# S3_REGION=auto
# S3_ACCESS_KEY_ID=toBeModified
# S3_SECRET_ACCESS_KEY=toBeModified
# S3_BUCKET=toBeModified
# S3_BASE_URL=https://cdn.yourdomain.com

# --- Supabase Storage ---
# SUPABASE_URL=https://<project>.supabase.co
# SUPABASE_SERVICE_ROLE_KEY=toBeModified
# SUPABASE_BUCKET=strapi-uploads
# SUPABASE_DIRECTORY=
